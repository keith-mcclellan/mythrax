[project]
name = "mythrax-mcp"
version = "0.1.0"
dependencies = [
    "fastmcp>=0.4.0",
    "httpx>=0.27.0",
]

[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"
